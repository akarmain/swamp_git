<h1 align="center">🌀 Swamp Git</h1>

<div style="text-align: center; max-width: 400px; margin: 0 auto;">
  <p>
    Архив записей <a href="https://akarmain.ru" target="_blank">akarmain</a>, созданный для создания иллюзии, что я провожу всё своё время за программированием. 📚💻
  </p>
</div>

<p align="center">
  <img src="https://media.giphy.com/media/JIX9t2j0ZTN9S/giphy.gif" alt="Котик работает" width="400">
</p>

## 🚀 Быстрая установка на сервер

### 1️⃣ Клонируйте репозиторий на сервер
```bash
git clone git@github.com:akarmain/swamp_git.git
cd swamp_git
```

### 2️⃣ Настройте конфигурацию
```bash
# Скопируйте пример конфигурации
cp exemple.env .env

# Отредактируйте .env и заполните:
nano .env
```

**Обязательные параметры в .env:**
- `OPENAI_API_KEY` - ваш API ключ для AI
- `REPO_URL` - SSH URL вашего репозитория (git@github.com:USER/REPO.git)
- `GIT_AUTHOR_NAME` - ваше имя для коммитов
- `GIT_AUTHOR_EMAIL` - ваш email для коммитов

### 3️⃣ Запустите установку

**Для systemd (Ubuntu, Debian, CentOS):**
```bash
chmod +x install.sh
./install.sh
```

**Для cron (универсальный метод):**
```bash
chmod +x install_cron.sh
./install_cron.sh
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение Python
- ✅ Установит все зависимости
- ✅ Настроит systemd timer для запуска каждый день в **4:20 МСК**
- ✅ Запустит автоматические коммиты

### 4️⃣ Проверьте работу
```bash
# Тестовый запуск
./test_run.sh

# Просмотр логов
sudo journalctl -u swamp-git.service -f

# Статус timer
sudo systemctl status swamp-git.timer

# Когда следующий запуск
systemctl list-timers | grep swamp-git
```

## 📋 Ручное использование

### Разовый AI-коммит на сегодшний день
```bash
source .venv/bin/activate
python3 source/swamp_git.py gpt-push --count 1
```

### Несколько коммитов с задержкой
```bash
python3 source/swamp_git.py gpt-push --count 5 --delay-sec 30
```

### Заполнить пропущенные даты
```bash
python3 source/swamp_git.py fill-missing 2025-10-01
python3 source/swamp_git.py fill-missing YYYY-MM-DD,YYYY-MM-DD

```

### Бэктейтинг (коммиты в прошлом)
```bash
# По одному коммиту за последние 7 дней
python3 source/swamp_git.py gpt-push --count 7 --backdate daily
```

## 🛠️ Управление

### Остановить автоматические коммиты
```bash
sudo systemctl stop swamp-git.timer
sudo systemctl disable swamp-git.timer
```

### Возобновить автоматические коммиты
```bash
sudo systemctl enable swamp-git.timer
sudo systemctl start swamp-git.timer
```

### Удалить timer (но оставить проект)
```bash
chmod +x uninstall.sh
./uninstall.sh
```

## ⚙️ Изменение времени запуска

По умолчанию коммиты делаются в **4:20 МСК** (01:20 UTC). Чтобы изменить:

```bash
# Редактируем timer
sudo nano /etc/systemd/system/swamp-git.timer

# Измените строку OnCalendar (например, для 12:00 МСК = 09:00 UTC):
OnCalendar=*-*-* 09:00:00

# Перезагрузите systemd
sudo systemctl daemon-reload
sudo systemctl restart swamp-git.timer
```

## 📦 Зависимости

Все зависимости перечислены в `requirements.txt`:
- gitpython - работа с git
- loguru - логирование
- python-dateutil - работа с датами
- pytz - часовые пояса
- openai - AI для генерации коммитов
- python-dotenv - загрузка .env
- requests - HTTP запросы

## 🎯 Особенности

- ✨ AI-генерация коммит-сообщений на тему QR генератора
- ✍️ Возможно нужно будет изменить AI_PROMPT в `swamp_git.py`
- 📅 Автоматический запуск по расписанию (4:20 МСК)
- 🕐 Бэктейтинг коммитов в прошлое
- 🔒 SSH аутентификация в GitHub
- 📊 Логирование всех операций
- 🔄 Два способа установки: systemd или cron

## 🤝 Поддержка

По вопросам обращайтесь к 

<div align="center">
    ↝Спасибо, что читаете [мой](https://akarmain.ru) код!↝
</div>
