# Swamp Git - Инструкция по установке на сервер

## Что делает этот проект?

Автоматически создает коммиты в ваш GitHub репозиторий каждый день в 4:20 МСК с помощью AI-генерации сообщений.

## Установка на сервер

### Шаг 1: Клонирование на сервер

```bash
# Подключитесь к серверу по SSH
ssh user@your-server.com

# Клонируйте репозиторий
git clone git@github.com:akarmain/swamp_git.git
cd swamp_git
```

### Шаг 2: Настройка .env файла

```bash
# Скопируйте пример
cp exemple.env .env

# Откройте для редактирования
nano .env
```

**Измените следующие параметры:**

```env
# ОБЯЗАТЕЛЬНО ЗАПОЛНИТЕ ЭТИ ПОЛЯ:
OPENAI_API_KEY=ваш_api_ключ_здесь
REPO_URL=git@github.com:ваш_юзернейм/swamp_git.git
GIT_AUTHOR_NAME=Ваше Имя
GIT_AUTHOR_EMAIL=your@email.com

# Необязательные (можно оставить как есть):
OPENAI_BASE_URL=https://api.proxyapi.ru/deepseek/
OPENAI_MODEL=deepseek-chat
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 3: Запуск установки

```bash
# Сделайте скрипт исполняемым (если еще не сделано)
chmod +x install.sh

# Запустите установку
./install.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение Python
- Установит все зависимости
- Настроит systemd timer для запуска в 4:20 МСК каждый день
- Запустит timer

### Шаг 4: Проверка

```bash
# Тестовый запуск (создаст один коммит прямо сейчас)
./test_run.sh

# Проверить статус timer
sudo systemctl status swamp-git.timer

# Посмотреть когда следующий запуск
systemctl list-timers | grep swamp-git

# Посмотреть логи
sudo journalctl -u swamp-git.service -f
```

## Готово! 🎉

Теперь каждый день в 4:20 по МСК ваш сервер будет автоматически:
1. Генерировать AI сообщение коммита
2. Создавать файл с активностью
3. Делать коммит
4. Пушить в GitHub

## Дополнительные команды

### Остановить автоматические коммиты
```bash
sudo systemctl stop swamp-git.timer
sudo systemctl disable swamp-git.timer
```

### Возобновить автоматические коммиты
```bash
sudo systemctl enable swamp-git.timer
sudo systemctl start swamp-git.timer
```

### Удалить timer
```bash
./uninstall.sh
```

### Ручной запуск (без ожидания 4:20)
```bash
source .venv/bin/activate
python source/swamp_git.py gpt-push --count 1
```

### Создать несколько коммитов сразу
```bash
source .venv/bin/activate
# 5 коммитов с задержкой 30 секунд между ними
python source/swamp_git.py gpt-push --count 5 --delay-sec 30
```

### Заполнить пропущенные даты
```bash
source .venv/bin/activate
python source/swamp_git.py fill-missing 2025-10-01,2025-10-02,2025-10-04
```

## Изменение времени запуска

Чтобы изменить время с 4:20 МСК на другое:

```bash
# Редактируем timer файл
sudo nano /etc/systemd/system/swamp-git.timer

# Найдите строку OnCalendar и измените время
# Примеры:
# 12:00 МСК = 09:00 UTC
OnCalendar=*-*-* 09:00:00

# Сохраните и перезагрузите
sudo systemctl daemon-reload
sudo systemctl restart swamp-git.timer
```

## Поиск проблем

### Проверить что timer работает
```bash
systemctl list-timers --all | grep swamp-git
```

### Посмотреть последние логи
```bash
sudo journalctl -u swamp-git.service -n 50
```

### Проверить что .env правильный
```bash
cat .env
# Убедитесь что все ключи заполнены
```

### Ручной тест
```bash
cd /path/to/swamp_git
source .venv/bin/activate
python source/swamp_git.py gpt-push --count 1
# Если работает - значит проблема в timer, если нет - проблема в настройках
```

## Требования

- Linux сервер с systemd (Ubuntu, Debian, CentOS, etc.)
- Python 3.7+
- SSH доступ к серверу
- SSH ключ настроен для GitHub (для git push)
- API ключ OpenAI или совместимого сервиса

---

**Готово!** Теперь ваш GitHub профиль будет выглядеть активным каждый день! 🌿
